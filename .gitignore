# Environments
.venv/
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.pytest_cache/

# Secrets & credentials WRDS
.env
.pgpass
*.pgpass


# LaTeX appendix glue for the Overleaf write-up — lives with the thesis,
# not with the codebase
appendix.tex

# Data (under WRDS license — NEVER on git)
data/raw/*
data/processed/*
!data/raw/.gitkeep
!data/processed/.gitkeep

# Generated results (figures + tables): tracked on git — report deliverables

# OS / IDE
.DS_Store
.vscode/
.idea/
